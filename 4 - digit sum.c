#include<stdio.h>

main()
{
	int  n,first,sum;
	printf("Enter Value =");
    scanf("%d",&n);
    while(n>0)
    {
    	first= n
    	sum= first+last;
	}
	    printf("first and last digit sum = %d",sum);
}
